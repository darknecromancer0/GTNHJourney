# GTNH Journey 1.1.25 Final Audit Plan

## Goal

Close the remaining gaps discovered after the nominal 1.1.24 release before asking for another live test.

## Required fixes

1. Replace the Journey-only chat hint source with server-authoritative completion for every slash command registered on the GTNH server.
2. Display live completion while typing; Up/Down changes the highlighted entry and Tab accepts the selected entry without invoking vanilla chat-history navigation/completion.
3. Keep ordinary vanilla chat history behaviour when the suggestion popup is empty.
4. Ensure the help surface includes all 1.1.24 command branches: undo/redo counts, speed default/undo/redo, explosions default/undo/redo/machines, death inventory recovery, and external snapshot return.
5. Re-audit the previously requested NEI page retention, native subset/search delegation, J/N/F/C click semantics, debug researcher press latch, spawner identity, dropped-item AREA_16 scan, death inventory guard, safe machine speed and external snapshots through structural/regression contracts.
6. Bump runtime/Gradle/mcmod versions together to 1.1.25 only after the full test gate is green.

## Completion gate

- GTNH formatting succeeds.
- Full Forge build succeeds.
- All unit and structural regression tests succeed.
- Artifact version verification reports 1.1.25.
- Only that exact CI artifact is handed to the user.
